# Demo mode: set to true for frontend-only demo (no real API or payments)
VITE_DEMO_MODE=true

# Optional: only needed when NOT in demo mode
# VITE_ENVIRONMENT=development

# Optional: Paymentez (only when NOT in demo mode and using real payments)
# VITE_PAYMENTEZ_ENV=stg
# VITE_PAYMENTEZ_APP_CODE=
# VITE_PAYMENTEZ_APP_KEY=
